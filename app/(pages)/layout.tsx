// STUB — Chat 1 (Frontend) owns /app/(pages)/.
// This file marks the (pages) route group.
// Page routes will be added here: home, deck, review session, etc.

export default function PagesLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
