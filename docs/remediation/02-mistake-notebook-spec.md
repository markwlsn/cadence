# Cross-Deck Mistake Notebook Architecture

Centralized error registry stored in local client storage (`cadence_mistake_notebook`).

## Capabilities
- Automatic logging of any card rated `Again` or `Hard`.
- Filter by deck, error taxonomy, and resolution state.
- 1-click targeted remediation drill.
