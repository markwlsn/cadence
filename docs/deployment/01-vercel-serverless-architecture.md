# Vercel Serverless Architecture

- Next.js 16 App Router optimized for Vercel Edge and Serverless functions.
- Automatic Prisma Client generation on install via `postinstall` script.
- Resilient mock fallbacks ensure zero 500 crashes in uninitialized cloud environments.
