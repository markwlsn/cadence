# Offline Resilience Protocol

- Review sessions persist across page refreshes via `sessionStorage`.
- Mistake notebook and assessment milestones persist in `localStorage`.
- Mock fallbacks activate automatically if network connectivity is interrupted.
